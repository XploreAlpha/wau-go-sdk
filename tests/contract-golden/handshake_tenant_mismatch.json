{
  "scenario": "handshake_tenant_mismatch",
  "endpoint": "GET /v0.8.0/handshake/sessions/{session_id}",
  "request": {
    "session_id": "wrong-tenant-sess",
    "query": {"tenant_id": "tenant-B"}
  },
  "expected_response": {
    "http_status": 403,
    "error_code": -32003,
    "error_message_pattern": "tenant does not own"
  },
  "expected_sdk_mapping": {
    "go": "ErrHandshakeTenantMismatch",
    "python": "HandshakeTenantMismatchError",
    "typescript": "HandshakeTenantMismatchError",
    "rust": "Error::Handshake(msg with -32003)"
  },
  "notes": "v0.8.0 M5-1 B.1 — 跨 tenant 访问 session 4 SDK 错误映射表"
}
